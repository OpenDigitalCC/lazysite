# Form dispatch handlers
# Add handlers here and reference them from form .conf files

handlers:
  - id: local-storage
    enabled: true
    name: Local file storage
    path: lazysite/forms/submissions
    type: file
