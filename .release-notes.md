Release 0.4.71 - Gate password-reset link on SMTP

Full suite green (1815); perlcritic clean; strict SBOM gate green.

The login page's "Forgot password?" link is now shown only when SMTP is configured
(lazysite/forms/smtp.conf present). The emailed-reset path was already gated on
that server-side; the link no longer offers a reset that cannot be sent. The
processor exposes a smtp_configured flag to pages for the guard.
