Release 0.9.7 - BETA: submissions-button fix + domains/site-package UX pass

Channel: BETA. A bug fix plus a UX polish pass on the 0.9.5/0.9.6 beta line. No
BREAKING changes, no migration.

Fix:
- The plugin-config "View submissions" button (SM182, 0.9.5) did nothing and
  logged a SyntaxError - its onclick was a double-quoted attribute with a raw
  JSON.stringify inside, so it terminated the attribute early. Rebuilt on the
  file's safe single-quoted pattern.

SM185 - domains + site-package UX pass:
- lang/lang_group now travel in a site package (created + applied), so an
  exported/migrated site keeps its language.
- "Export this site" on Backups > Site packages packages the DEFAULT site without
  the domains feature (manage_content), excluding lazysite/ and other domains.
- The Domains page is about ADDITIONAL domains only (the default site is no longer
  listed); per-row actions fold into an Actions dropdown; copy reframed.
- The service killswitch toggles sit under a single Services heading.

Also recorded: SM184 (publish pages by email) as a candidate proposal (doc only).

Full gate before the build: suite + bench + coverage (every production CGI above
floors).
