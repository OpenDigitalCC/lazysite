Release 0.5.21 - Group descriptions + audited group changes + sub-user ordering

Full suite green; perlcritic clean; strict SBOM gate green.  Channel: edge.

- Each group now has a free-text description (what the role is for), editable on
  the Groups page.
- Group capability and membership changes are recorded in the audit trail with the
  group as the target (user@group for membership, the group for settings) - so you
  can see exactly which role changed. (The audit still captures every material
  change after the clean cut - validated.)
- The Users page shows an account's sub-users at the top of its card.
