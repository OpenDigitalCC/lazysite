Release 0.5.5 - STABLE consolidation + skipped-site repair

Full suite green; perlcritic clean; strict SBOM gate green.  Channel: STABLE.

- Repairs sites that were channel-skipped: the deploy used to run the pre-install
  chown but bail on a skip BEFORE the corrective permission pass, leaving skipped
  stable sites half-configured (manager 500s). Cutting this STABLE makes those
  sites upgrade (not skip), so the full deploy - including the permission pass -
  runs and fixes them.
- Deploy now runs install.pl --channel-check FIRST, before the template apply and
  the chown, and bails on a skip - so a future skip is a true no-op.
- Includes: submissions are readable in the manager again (0.5.4); a form errors
  instead of a false "thank you" when no target accepts it (0.5.2) or the
  submission is blank (0.5.3); login layout, self-service link, webdav gate,
  logout-audit, json: UTF-8.

Deploy this STABLE to all sites; the two stable-channel sites will take it (it is
stable) and be repaired.
