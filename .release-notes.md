Release 0.7.11 - STABLE: the ladder reaches Site settings + a machine-readable backlog

Channel: STABLE - the whole fleet takes this on the next upgrade run,
folding in everything since 0.7.8 (the 0.7.9 SM095 sweep and the
0.7.10 beta: channel ladder, one-switch Content history, MCP version
tools, agent doc currency).

Channel ladder completes its UI surface (field finding on 0.7.10)
- The Site settings Update channel selector - the surface most
  operators actually use - still offered only all/stable, so the beta
  rung could not be selected from the manager. It now offers
  all / beta / stable with the ladder explained in its note; the
  control API's config-set accepts all|edge|beta|stable ('edge' is the
  CLI's synonym of 'all', so CLI-set sites round-trip); the update-
  channel docs teach the full three-rung acceptance table. Tests pin
  every accepted value.

The backlog is machine-readable now
- Every feature-request doc carries a status header (shipped, partial,
  parked, candidate or superseded) with a note on the non-shipped
  ones; tools/backlog.pl lists the open work; a lint gate refuses any
  new SM doc without a status. SM107 closed as superseded (SM138
  retired the conf key it targeted).

Rolling up from 0.7.9/0.7.10 for stable-channel sites
- Silent-failure guarantees and the SM095 permission-language sweep
  (group-set shell verb, honest Users-page account creation).
- The edge < beta < stable channel ladder itself.
- Content history as ONE switch (plugin tick = recording; untick =
  paused, versions kept) + the last-plugin lazysite.conf fix.
- MCP version-history tools (list/view/restore) and the agent tool
  reference brought current (37 tools); layout-switch order taught.

Fleet: sudo lazysite upgrade --all.
