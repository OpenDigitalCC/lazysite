Release 0.5.34 - Appearance backups listed newest-first

Full suite green (1999 tests); perlcritic clean; strict SBOM gate green.  Channel: edge.

The layout/theme backups panel on the Appearance page now lists snapshots with the
most recent at the top (sorted by the timestamp in each snapshot's name), rather
than grouped by layout in alphabetical order.
