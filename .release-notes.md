Release 0.5.0 - Stable consolidation release

Full suite green; perlcritic clean; strict SBOM gate green.

First release cut on the STABLE channel (release manifest channel = stable). It
consolidates all the work since the last stable point - manager bar / cache fixes
(including the homepage bar), the json: UTF-8 fix, search-index and visitor-stats
security hardening, the update channel, and form binary uploads + SMTP attachments.

Because it is stable, the two stable-channel sites WILL install it (they had been
correctly skipping the intervening edge releases). Deploy as usual; all 14 sites
take it.
