Release 0.5.9 - Backups collapse panel + batch delete

Full suite green; perlcritic clean; strict SBOM gate green.  Channel: edge.

The Appearance "installed" list no longer fills with old -backup-<ts> snapshots.
Every backup now folds into a single collapsed "Backups (N)" panel below the real
layouts, with per-item Delete and a "Delete all backups" button. The active layout
and theme are never touched. (Pairs with 0.5.8, which stopped backups chaining and
let you actually delete them.)
