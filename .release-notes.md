Release 0.4.72 - Manager-aware login landing

Full suite green (1819); perlcritic clean; strict SBOM gate green.

A recognised manager who logs in without an explicit next (so it defaulted to
home) now lands in the manager UI - addressing "logged in but couldn't find the
manager". Gated on the manager UI being enabled; an explicit next (a real
bounce-from page) is always honoured; non-managers land on home as before.
