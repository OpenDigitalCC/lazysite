Release 0.4.45 - Auth audit completeness + self-service/OAuth docs

Verification, audit completeness, and documentation for the self-service credential
and OAuth features (both already built). Full suite green; perlcritic clean; strict
SBOM gate green.

## Verified: SM072 and SM076 are done

Self-service credentials + TOTP MFA (SM072) and OAuth 2.1 for the MCP (SM076) are fully
implemented, tested, and audited - all four SM072 batches (claim primitive, email
forgot-password, partner exchange, TOTP) and all five OAuth stages. Stale doc statuses
("draft, no implementation" and an earlier note calling OAuth "future") were corrected.

## Audit names the plugin (and the reset)

plugin-enable / disable / save audited '/' as their target; they now name the plugin
(reported: "plugin-enable | / - didn't say which plugin"). And /forgot records a forgot
audit event when a real reset link is emailed (the HTTP response stays generic). The
claim / token-exchange / mfa-enrol / oauth lifecycle events were already audited.

## Docs

starter/docs/auth.md gains a "Self-service credentials and two-factor" section: setup
links, reset credential, forgot-password (email), TOTP enrolment and the login second
factor, account expiry, and the security + audit model. The OAuth connector flow is
already documented in ai-connector-setup.md.

## Version

bump-version.pl promoted VERSION to 0.4.45.
