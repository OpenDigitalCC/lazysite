Release 0.5.16 - Users page fixes (group list, load speed, grid)

Full suite green; perlcritic clean; strict SBOM gate green.  Channel: edge.

- The Users page now lists every group, including the seeded role groups that have
  no members yet - so you can actually assign people to content-editors,
  design-team, agent-ai, mcp-ai and user-managers.
- The page loads all accounts + their settings in one call instead of one
  subprocess per account, fixing the slow load.
- Permission grid: channel headers centre over the ticks.
