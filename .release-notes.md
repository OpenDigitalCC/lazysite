Release 0.6.10 - Backlog housekeeping + SM141 sessions scoping

A documentation release closing out the week's planning work ahead of the
eight-dimension review cycle. All gates green; channel: edge.

- Backlog housekeeping: thirteen shipped items moved to Done (SM096/098/103/
  126/128/136/138/140 + the manager-UX/ops batch); SM139 (packaged
  distribution) promoted to Open - actionable, NEXT UP; the visitor-stats
  performance entry closed as largely superseded by SM140.
- SM141 scoping: Sessions page (list + control active sessions). Recommended
  design keeps signed cookies as the source of truth and adds a 24h
  self-pruning login registry + a fail-closed revocation list (per-session and
  per-user sign-out without global secret rotation). Additive, not breaking;
  sequenced after the review and the 0.7.0 stable cut.
