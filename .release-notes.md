Release 0.6.9 - AI visitor analytics reads the first-party log

Completes SM140: the analyse_visitors AI-connector surface joins the stats
page on the first-party access log. All gates green; channel: edge.

First-party AI export (SM140 increments 3+4)
- analyse_visitors / the stats export ingests lazysite's own access log when
  present - per-day-file byte offsets into the same incremental day-bucket
  cache, so repeated calls stay cheap and only new lines are read. The
  web-server log remains the fallback; the export's `source` field says which.
- Manager/operator traffic is excluded; the anonymised daily visitor key is
  the event identity - no raw IPs anywhere in the export.
- Docs updated (manager guide, AI briefing, plugin self-description). Channel
  tags for forms/DAV are deferred by decision until a consumer exists.
