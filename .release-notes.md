Release 0.4.63 - Fix: Visitor statistics nav hidden while enabled

Patch over 0.4.62. Full suite green (1780); perlcritic clean; SBOM gate green.

The conditional "Visitor statistics" nav item could remain hidden even when the
stats plugin was enabled. The layout conditional and var wiring were correct (now
covered by a regression test that renders the real manager layout); enabled-plugin
detection is hardened to resolve the plugin id from any lazysite.conf entry form
(stats.pl, a path-prefixed entry, or a bare id).

## Version

bump-version.pl promoted VERSION to 0.4.63.
